<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  {% include 'scripts.twig' %}
  {% include 'meta.twig' %}
  <style>[x-cloak] { display: none !important }</style>
</head>

<body class="{{ env.devMode ? 'debug-screens' }}">
  {% include 'header.twig' %}
  {% block body %}
    {{ content|raw }}
  {% endblock %}
  {% include 'footer.twig' %}
</body>

</html>
